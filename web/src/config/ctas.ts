/** Primary / secondary CTA presets — pages override sparingly. */
export const ctas = {
  launchDashboard: {
    label: "Launch Dashboard",
    href: "/dashboard",
  },
  addSource: {
    label: "Add Source",
  },
  discoverAll: {
    label: "Discover All",
  },
} as const;
